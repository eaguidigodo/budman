<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>

<div class="container login-container">
    <div class="row">

        <div class="col-md-8 login-form-2  offset-md-2">
            <h3>Se connecter</h3>
            <form>
                <div class="form-group">
                  <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder:"Votre email*", class: "form-control" %>
                </div>

                <div class="form-group">
                  <%= f.password_field :password, autocomplete: "current-password", placeholder:"Votre mot de passe*", class: "form-control" %>
                </div>

                <% if devise_mapping.rememberable? %>
                    <div class="form-group">
                      <%= f.check_box :remember_me, class:"text-white" %>
                      <%= f.label :remember_me%>
                    </div>
                <% end %>

                <div class="form-group">
                    <%= f.submit "Se connecter", class:"btnSubmit" %>
                </div>



                <div class="form-group">
                <p class:"ForgetPwd"> Vous n'avez pas encore de comptet? <%= link_to "S'inscrire", new_user_registration_path, class:"ForgetPwd" %></p>
                </div>
            </form>
    </div>
</div>
  </div>

  <% end %>

<%= render "devise/shared/links" %>
